apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: html-gitops-eks
  namespace: stage
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: stage.lynasovann.site
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: html-gitops-eks
                port:
                  number: 80
